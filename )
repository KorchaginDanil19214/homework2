squares 0 b c = ( x1, x1)
                 where 
				 x1= -c/b
squares 0 0 c = error "it's not a square equation"
squares a b c | d < 0 = error "there are no roots"
              | d == 0 = (x1,x1) 
              | otherwise = (x1, x2) 
               where 
                 d = b*b - 4*a*c
                 x1 = (-b + sqrt (d))/(2*a)
                 x2 = (-b - sqrt (d))/(2*a)
